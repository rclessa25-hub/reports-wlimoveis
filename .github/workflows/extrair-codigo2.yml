name: 📥 Extrair Código Fonte Ver2

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  extract-code:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
    
    - name: 📥 Clonar projeto
      run: |
        echo "📥 CLONANDO CÓDIGO FONTE..."
        git clone https://github.com/rclessa25-hub/imoveis-maceio.git fonte-completo
    
    - name: 📝 Criar relatório sem HEREDOC
      run: |
        echo "📄 GERANDO RELATÓRIO SEM HEREDOC..."
        
        DATE_NOW=$(date '+%d/%m/%Y às %H:%M:%S')
        
        # Método SIMPLES para contar
        echo "Calculando estatísticas..."
        
        # Inicializar contadores
        HTML_COUNT=0
        JS_COUNT=0
        CSS_COUNT=0
        JSON_COUNT=0
        TOTAL_LINES=0
        TOTAL_FUNCTIONS=0
        
        # Processar cada tipo separadamente
        # HTML
        for file in fonte-completo/*.html; do
          if [ -f "$file" ]; then
            HTML_COUNT=$((HTML_COUNT + 1))
            L=$(wc -l < "$file")
            TOTAL_LINES=$((TOTAL_LINES + L))
          fi
        done
        
        # CSS
        if [ -d "fonte-completo/css" ]; then
          for file in fonte-completo/css/*.css; do
            if [ -f "$file" ]; then
              CSS_COUNT=$((CSS_COUNT + 1))
              L=$(wc -l < "$file")
              TOTAL_LINES=$((TOTAL_LINES + L))
            fi
          done
        fi
        
        # JavaScript
        # JS na raiz
        for file in fonte-completo/js/*.js; do
          if [ -f "$file" ]; then
            JS_COUNT=$((JS_COUNT + 1))
            L=$(wc -l < "$file")
            TOTAL_LINES=$((TOTAL_LINES + L))
            # Contar funções
            if grep -q "function" "$file"; then
              F=$(grep -c "function" "$file")
              TOTAL_FUNCTIONS=$((TOTAL_FUNCTIONS + F))
            fi
          fi
        done
        
        # Módulos JS
        if [ -d "fonte-completo/js/modules" ]; then
          for file in fonte-completo/js/modules/*.js; do
            if [ -f "$file" ]; then
              JS_COUNT=$((JS_COUNT + 1))
              L=$(wc -l < "$file")
              TOTAL_LINES=$((TOTAL_LINES + L))
              if grep -q "function" "$file"; then
                F=$(grep -c "function" "$file")
                TOTAL_FUNCTIONS=$((TOTAL_FUNCTIONS + F))
              fi
            fi
          done
        fi
        
        # Componentes JS
        if [ -d "fonte-completo/js/components" ]; then
          for file in fonte-completo/js/components/*.js; do
            if [ -f "$file" ]; then
              JS_COUNT=$((JS_COUNT + 1))
              L=$(wc -l < "$file")
              TOTAL_LINES=$((TOTAL_LINES + L))
              if grep -q "function" "$file"; then
                F=$(grep -c "function" "$file")
                TOTAL_FUNCTIONS=$((TOTAL_FUNCTIONS + F))
              fi
            fi
          done
        fi
        
        # JSON
        for file in fonte-completo/*.json; do
          if [ -f "$file" ]; then
            JSON_COUNT=$((JSON_COUNT + 1))
            L=$(wc -l < "$file")
            TOTAL_LINES=$((TOTAL_LINES + L))
          fi
        done
        
        TOTAL_FILES=$((HTML_COUNT + JS_COUNT + CSS_COUNT + JSON_COUNT))
        
        # CRIAR HTML USANDO APENAS ECHO - SEM HEREDOC
        echo "Criando HTML sem HEREDOC..."
        
        # Criar arquivo vazio
        > index.html
        
        # Adicionar header com echo
        echo '<!DOCTYPE html>' >> index.html
        echo '<html lang="pt-BR">' >> index.html
        echo '<head>' >> index.html
        echo '<meta charset="UTF-8">' >> index.html
        echo '<meta name="viewport" content="width=device-width, initial-scale=1.0">' >> index.html
        echo '<title>📥 Código Fonte - Imóveis Maceió</title>' >> index.html
        echo '<style>' >> index.html
        echo 'body { font-family: Arial, sans-serif; background: #0d1117; color: white; margin: 0; padding: 20px; }' >> index.html
        echo '.container { max-width: 1400px; margin: 0 auto; }' >> index.html
        echo 'header { text-align: center; padding: 30px 0; border-bottom: 3px solid #2ea44f; margin-bottom: 30px; }' >> index.html
        echo 'h1 { color: #2ea44f; font-size: 2.5em; }' >> index.html
        echo 'h2 { color: #58a6ff; margin: 30px 0 20px; }' >> index.html
        echo '.file-list { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 15px; margin: 20px 0; }' >> index.html
        echo '.file-card { background: #161b22; border: 1px solid #30363d; border-radius: 8px; padding: 20px; }' >> index.html
        echo '.file-card h3 { margin-top: 0; color: #58a6ff; }' >> index.html
        echo '.code-viewer { background: #000; padding: 20px; border-radius: 8px; margin: 20px 0; overflow-x: auto; }' >> index.html
        echo 'pre { margin: 0; font-family: "Monaco", monospace; font-size: 0.9em; line-height: 1.4; }' >> index.html
        echo '.line-number { color: #6a737d; margin-right: 15px; user-select: none; }' >> index.html
        echo '</style>' >> index.html
        echo '</head>' >> index.html
        echo '<body>' >> index.html
        echo '<div class="container">' >> index.html
        echo '<header>' >> index.html
        echo '<h1>📥 Código Fonte Completo</h1>' >> index.html
        echo '<p>Imóveis Maceió - Extração completa</p>' >> index.html
        echo '<p><strong>Gerado em:</strong> '"$DATE_NOW"'</p>' >> index.html
        echo '</header>' >> index.html
        
        # Adicionar resumo
        echo '<h2>📊 Resumo da Extração</h2>' >> index.html
        echo '<div style="background: #161b22; padding: 20px; border-radius: 8px; margin: 20px 0;">' >> index.html
        echo '<p><strong>Total de arquivos:</strong> '"$TOTAL_FILES"'</p>' >> index.html
        echo '<p><strong>Total de linhas:</strong> '"$TOTAL_LINES"'</p>' >> index.html
        echo '<p><strong>Funções JavaScript:</strong> '"$TOTAL_FUNCTIONS"'</p>' >> index.html
        echo '<p><strong>HTML:</strong> '"$HTML_COUNT"' arquivos</p>' >> index.html
        echo '<p><strong>JavaScript:</strong> '"$JS_COUNT"' arquivos</p>' >> index.html
        echo '<p><strong>CSS:</strong> '"$CSS_COUNT"' arquivos</p>' >> index.html
        echo '<p><strong>JSON:</strong> '"$JSON_COUNT"' arquivos</p>' >> index.html
        echo '</div>' >> index.html
        
        # Lista de arquivos
        echo '<h2>📁 Lista de Arquivos Extraídos</h2>' >> index.html
        echo '<div class="file-list">' >> index.html
        
        # Adicionar cada arquivo com echo
        # HTML
        for file in fonte-completo/*.html; do
          if [ -f "$file" ]; then
            filename=$(basename "$file")
            lines=$(wc -l < "$file")
            size=$(wc -c < "$file")
            size_kb=$((size / 1024))
            echo '<div class="file-card">' >> index.html
            echo '<h3>📄 '"$filename"'</h3>' >> index.html
            echo '<p><strong>Local:</strong> .</p>' >> index.html
            echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
            echo '<p><strong>Funções:</strong> N/A (HTML)</p>' >> index.html
            echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
            echo '</div>' >> index.html
          fi
        done
        
        # CSS
        if [ -d "fonte-completo/css" ]; then
          for file in fonte-completo/css/*.css; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              lines=$(wc -l < "$file")
              size=$(wc -c < "$file")
              size_kb=$((size / 1024))
              echo '<div class="file-card">' >> index.html
              echo '<h3>🎨 '"$filename"'</h3>' >> index.html
              echo '<p><strong>Local:</strong> css</p>' >> index.html
              echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
              echo '<p><strong>Funções:</strong> N/A (CSS)</p>' >> index.html
              echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
              echo '</div>' >> index.html
            fi
          done
        fi
        
        # JavaScript
        # JS na raiz
        for file in fonte-completo/js/*.js; do
          if [ -f "$file" ]; then
            filename=$(basename "$file")
            lines=$(wc -l < "$file")
            size=$(wc -c < "$file")
            size_kb=$((size / 1024))
            functions=0
            if grep -q "function" "$file"; then
              functions=$(grep -c "function" "$file")
            fi
            echo '<div class="file-card">' >> index.html
            echo '<h3>📜 '"$filename"'</h3>' >> index.html
            echo '<p><strong>Local:</strong> js</p>' >> index.html
            echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
            echo '<p><strong>Funções:</strong> '"$functions"'</p>' >> index.html
            echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
            echo '</div>' >> index.html
          fi
        done
        
        # Módulos JS
        if [ -d "fonte-completo/js/modules" ]; then
          for file in fonte-completo/js/modules/*.js; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              lines=$(wc -l < "$file")
              size=$(wc -c < "$file")
              size_kb=$((size / 1024))
              functions=0
              if grep -q "function" "$file"; then
                functions=$(grep -c "function" "$file")
              fi
              echo '<div class="file-card">' >> index.html
              echo '<h3>📜 '"$filename"' (Módulo)</h3>' >> index.html
              echo '<p><strong>Local:</strong> js/modules</p>' >> index.html
              echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
              echo '<p><strong>Funções:</strong> '"$functions"'</p>' >> index.html
              echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
              echo '</div>' >> index.html
            fi
          done
        fi
        
        # Componentes JS
        if [ -d "fonte-completo/js/components" ]; then
          for file in fonte-completo/js/components/*.js; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              lines=$(wc -l < "$file")
              size=$(wc -c < "$file")
              size_kb=$((size / 1024))
              functions=0
              if grep -q "function" "$file"; then
                functions=$(grep -c "function" "$file")
              fi
              echo '<div class="file-card">' >> index.html
              echo '<h3>📜 '"$filename"' (Componente)</h3>' >> index.html
              echo '<p><strong>Local:</strong> js/components</p>' >> index.html
              echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
              echo '<p><strong>Funções:</strong> '"$functions"'</p>' >> index.html
              echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
              echo '</div>' >> index.html
            fi
          done
        fi
        
        # JSON
        for file in fonte-completo/*.json; do
          if [ -f "$file" ]; then
            filename=$(basename "$file")
            lines=$(wc -l < "$file")
            size=$(wc -c < "$file")
            size_kb=$((size / 1024))
            echo '<div class="file-card">' >> index.html
            echo '<h3>📋 '"$filename"'</h3>' >> index.html
            echo '<p><strong>Local:</strong> .</p>' >> index.html
            echo '<p><strong>Linhas:</strong> '"$lines"'</p>' >> index.html
            echo '<p><strong>Funções:</strong> N/A (JSON)</p>' >> index.html
            echo '<p><strong>Tamanho:</strong> '"$size_kb"' KB</p>' >> index.html
            echo '</div>' >> index.html
          fi
        done
        
        echo '</div>' >> index.html
        
        # Código completo
        echo '<h2>📜 Código Fonte Completo</h2>' >> index.html
        
        # Adicionar código de cada arquivo
        # HTML
        for file in fonte-completo/*.html; do
          if [ -f "$file" ]; then
            filename=$(basename "$file")
            lines=$(wc -l < "$file")
            echo '<div class="code-viewer">' >> index.html
            echo '<h3>'"$filename"' ('"$lines"' linhas)</h3>' >> index.html
            echo '<pre>' >> index.html
            line_num=1
            while IFS= read -r line; do
              # Escapar HTML
              line_escaped=$(echo "$line" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
              echo '<span class="line-number">'"$line_num"'</span>'"$line_escaped"'<br/>' >> index.html
              line_num=$((line_num + 1))
            done < "$file"
            echo '</pre>' >> index.html
            echo '</div>' >> index.html
          fi
        done
        
        # CSS
        if [ -d "fonte-completo/css" ]; then
          for file in fonte-completo/css/*.css; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              lines=$(wc -l < "$file")
              echo '<div class="code-viewer">' >> index.html
              echo '<h3>css/'"$filename"' ('"$lines"' linhas)</h3>' >> index.html
              echo '<pre>' >> index.html
              line_num=1
              while IFS= read -r line; do
                line_escaped=$(echo "$line" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
                echo '<span class="line-number">'"$line_num"'</span>'"$line_escaped"'<br/>' >> index.html
                line_num=$((line_num + 1))
              done < "$file"
              echo '</pre>' >> index.html
              echo '</div>' >> index.html
            fi
          done
        fi
        
        # Footer
        echo '<footer style="text-align: center; margin-top: 40px; padding-top: 20px; border-top: 1px solid #30363d; color: #8b949e;">' >> index.html
        echo '<p><strong>📥 Extração Completa do Código Fonte</strong></p>' >> index.html
        echo '<p>Todas as linhas extraídas sem limitações</p>' >> index.html
        echo '</footer>' >> index.html
        echo '</div>' >> index.html
        echo '</body>' >> index.html
        echo '</html>' >> index.html
        
        echo "✅ Relatório criado com sucesso!"
        echo ""
        echo "📊 ESTATÍSTICAS:"
        echo "==============="
        echo "Arquivos: $TOTAL_FILES"
        echo "Linhas: $TOTAL_LINES"
        echo "Funções JS: $TOTAL_FUNCTIONS"
        echo "HTML: $HTML_COUNT"
        echo "JavaScript: $JS_COUNT"
        echo "CSS: $CSS_COUNT"
        echo "JSON: $JSON_COUNT"
    
    - name: 📦 Upload para GitHub Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'
    
    - name: 🚀 Deploy
      uses: actions/deploy-pages@v4
    
    - name: 🎉 Mostrar resultado
      run: |
        echo ""
        echo "🎉 RELATÓRIO CRIADO COM SUCESSO!"
        echo "================================"
        echo ""
        echo "🌐 ACESSE EM:"
        echo "https://rclessa25-hub.github.io/reports-wlimoveis/"
        echo ""
        echo "✅ FUNCIONALIDADES:"
        echo "• 📁 Lista completa de arquivos"
        echo "• ⚡ Contagem de funções JavaScript"
        echo "• 📊 Estatísticas precisas"
        echo "• 📜 Código completo sem limitações"
        echo "• 🏷️ Categorização por tipo"
        echo "• 🎨 Design limpo e funcional"
